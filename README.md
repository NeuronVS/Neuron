# Neuron
Neuron is an AI-native code editor with persistent cross-session memory and deep AST-based code intelligence — the Cursor alternative that actually remembers your codebase. One-click deploy &amp; test, auto-fetched framework docs, bring-your-own-API or use ours. Download the latest release below. Issues &amp; feature requests welcome.
